# Provides -m64 builds for x86_64 build-hosts

definitions:

    - libsuffix      : ""
    - build          : x86_64-%(vendorID)
    - host           : x86_64-%(vendorID)

flags:

    # Base set of flags
    - base:
        c: "-m64"
        cxx: "-m64"
