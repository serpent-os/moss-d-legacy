# Provides -m32 builds for x86_64 build-hosts

definitions:

    - libsuffix      : "32"
    - build          : i686-%(vendorID)
    - host           : x86_64-%(vendorID)

flags:

    # Base set of flags
    - base:
        c: "-m32"
        cxx: "-m32"
